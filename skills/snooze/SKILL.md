---
description: Snooze the token quota — temporarily adds extra tokens to all limits for today
disable-model-invocation: true
---

Run this command and show the output to the user exactly as-is, with no extra commentary:

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/hooks/snooze.py
```
